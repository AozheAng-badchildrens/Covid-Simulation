/**
 * Write a description of class Delta here.
 * 
 * @author (your name) 
 * @version (a version number or a date)
 */
public class Delta extends Virus
{
    // instance variables - replace the example below with your own

    /**
     * Constructor for objects of class Delta
     */
    public Delta(int version, Person p, int radius, int infectionRate, boolean visible)
    {
        super(version, p, radius, infectionRate, visible);
    }
}
