/**
 * Write a description of class Omicron here.
 * 
 * @author (your name) 
 * @version (a version number or a date)
 */
public class Omicron extends Virus
{
    // instance variables - replace the example below with your own

    /**
     * Constructor for objects of class Omicron
     */
    public Omicron(int version, Person p, int radius, int infectionRate, boolean visible)
    {
        super(version, p, radius, infectionRate, visible);
    }
}
